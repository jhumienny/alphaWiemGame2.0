{
  "rules": {
    ".read": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'admin'",
    ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'admin'",
    "games": {
      ".read": true,
      ".write": true
    },
    "questions": {
      ".read": true,
      ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'admin'"
    },
    "playerReports": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'admin'",
      "$reportId": {
        ".write": "(auth != null && root.child('users').child(auth.uid).child('role').val() === 'admin') || (!data.exists() && newData.hasChildren(['gameId','context','reporterSlot','reporterName','reportedSlot','reportedName','status','createdAt']) && newData.child('status').val() === 'open' && (newData.child('context').val() === 'lobby' || newData.child('context').val() === 'game') && newData.child('gameId').isString() && newData.child('reporterSlot').isNumber() && newData.child('reportedSlot').isNumber() && newData.child('reporterSlot').val() !== newData.child('reportedSlot').val() && newData.child('reporterName').isString() && newData.child('reportedName').isString() && (!newData.child('reason').exists() || newData.child('reason').val() === 'obraźliwa nazwa' || newData.child('reason').val() === 'oszustwo' || newData.child('reason').val() === 'trolling'))"
      }
    },
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid && ((!data.exists() && newData.child('role').val() === 'user') || (data.exists() && newData.child('role').val() === data.child('role').val()))"
      }
    },
    "usernames": {
      "$username": {
        ".read": true,
        ".write": "auth != null && !data.exists() && newData.val() === auth.uid"
      }
    }
  }
}
